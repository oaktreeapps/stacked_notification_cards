name: stacked_notification_cards
description: This package gives iOS style stacked notification. And expand with a cool animaiton. It's also possible to slid each notificion or when they are grouped.

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_slidable: ^0.6.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
